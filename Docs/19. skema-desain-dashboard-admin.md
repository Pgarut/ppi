# Skema Desain — Dashboard Admin
## Sistem Informasi Madrasah — Flutter

> Berbeda dari role lain: Admin TIDAK memakai shared widget
> `DashboardTemplate` — punya implementasi custom sendiri di
> `dashboard_page.dart` miliknya (header gradient, stat card
> badge, feature card list-row). Task ini mengganti **Menu Fitur**
> dari layout list-row menjadi grid ikon bulat, supaya konsisten
> secara visual dengan WK/Guru Mapel/Kamad/Guru BK — walau
> sebelumnya Admin direncanakan pakai gaya berbeda (Mini Sidebar
> Dashboard). Keputusan ini sudah dikonfirmasi: **konsistensi
> visual lebih diprioritaskan.**

---

## 1. Status Prasyarat

| Komponen | Status |
|---|---|
| Header, Stat Grid, gradient hijau | **Tidak diubah** — Admin punya versi custom sendiri yang sudah bagus |
| Menu Fitur (list-row) | **DIUBAH** menjadi grid ikon bulat |
| Palet hijau/kuning/putih | Sudah ada di kode (`_green`, `_yellow`, `_white`, dst) — dipakai ulang |

---

## 2. Layout Sebelum → Sesudah

**Sebelum (list-row):**
```
┌───────────────────────────────────┐
│ [■] Master Data          →         │
│     Kelola data master             │
├───────────────────────────────────┤
│ [■] Absensi              →         │
│     Monitoring absensi             │
├───────────────────────────────────┤
│ ... (dst, 5 baris total)           │
└───────────────────────────────────┘
```

**Sesudah (grid ikon bulat, 3 kolom):**
```
┌─────┐ ┌─────┐ ┌─────┐
│ (●) │ │ (●) │ │ (●) │
│Master│ │Absen│ │Nilai│
│Data │ │si   │ │     │
└─────┘ └─────┘ └─────┘
┌─────┐ ┌─────┐
│ (●) │ │ (◆) │
│Rapor│ │Peng-│
│     │ │aturan│
└─────┘ └─────┘
```

---

## 3. Pembagian Warna 5 Menu

| Menu | Ikon | Warna | Alasan |
|---|---|---|---|
| Master Data | `storage_outlined` | 🟢 Hijau | Data operasional inti |
| Absensi | `calendar_today_outlined` | 🟢 Hijau | Monitoring rutin |
| Nilai | `grading_outlined` | 🟢 Hijau | Monitoring rutin |
| Rapor | `description_outlined` | 🟢 Hijau | Monitoring rutin |
| **Pengaturan** | `settings_outlined` | 🟡 **Kuning** | Bersifat konfigurasi sistem, berbeda dari 4 menu monitoring operasional lain — mengikuti pola "menu yang beda sifat = kuning" seperti Laporan/Pengaduan di role lain |

Kombinasi hex identik dengan role lain:

| Elemen | Hijau (4 menu) | Kuning (Pengaturan) |
|---|---|---|
| Gradasi background | `#E8F5E9 → #A5D6A7` (135°) | `#FFF8E1 → #FFE082` (135°) |
| Warna ikon | `#1B5E20` | `#E65100` |
| Shadow | `#2E7D32` @ 12%, blur 6px | `#F9A825` @ 12%, blur 6px |

---

## 4. Perubahan Struktur Kode

File: `lib/features/admin/dashboard/dashboard_page.dart`

Karena Admin tidak pakai `FeatureItem`/`_FeatureCard` shared, ubah
langsung method lokal `_buildFeatureGrid()` dan `_buildFeatureCard()`:

```dart
Widget _buildFeatureGrid() {
  final features = [
    _FeatureItem(Icons.storage_outlined, 'Master Data', 'Kelola data master', () => widget.onFeatureTap?.call('master-data')),
    _FeatureItem(Icons.calendar_today_outlined, 'Absensi', 'Monitoring absensi', () => widget.onFeatureTap?.call('absensi')),
    _FeatureItem(Icons.grading_outlined, 'Nilai', 'Monitoring nilai', () => widget.onFeatureTap?.call('nilai')),
    _FeatureItem(Icons.description_outlined, 'Rapor', 'Monitoring rapor', () => widget.onFeatureTap?.call('rapor')),
    _FeatureItem(Icons.settings_outlined, 'Pengaturan', 'Pengaturan sistem', () => widget.onFeatureTap?.call('pengaturan'), isSecondary: true),
  ];

  return GridView.builder(
    gridDelegate: const SliverGridDelegateWithFixedCrossAxisCount(
      crossAxisCount: 3,
      crossAxisSpacing: 12,
      mainAxisSpacing: 12,
      childAspectRatio: 1.0,
    ),
    shrinkWrap: true,
    physics: const NeverScrollableScrollPhysics(),
    itemCount: features.length,
    itemBuilder: (_, i) => _buildFeatureCard(features[i]),
  );
}

Widget _buildFeatureCard(_FeatureItem item) {
  final gradient = item.isSecondary
    ? [const Color(0xFFFFF8E1), const Color(0xFFFFE082)]
    : [const Color(0xFFE8F5E9), const Color(0xFFA5D6A7)];
  final iconColor = item.isSecondary ? const Color(0xFFE65100) : const Color(0xFF1B5E20);
  final shadowColor = item.isSecondary ? _yellow : _green;

  return Material(
    color: _white,
    borderRadius: BorderRadius.circular(16),
    child: InkWell(
      borderRadius: BorderRadius.circular(16),
      onTap: item.onTap,
      child: Container(
        decoration: BoxDecoration(
          borderRadius: BorderRadius.circular(16),
          border: Border.all(color: _green.withOpacity(0.1)),
        ),
        padding: const EdgeInsets.all(12),
        child: Column(
          mainAxisAlignment: MainAxisAlignment.center,
          children: [
            Container(
              width: 48, height: 48,
              decoration: BoxDecoration(
                shape: BoxShape.circle,
                gradient: LinearGradient(
                  begin: Alignment.topLeft,
                  end: Alignment.bottomRight,
                  colors: gradient,
                ),
                boxShadow: [
                  BoxShadow(color: shadowColor.withOpacity(0.12), blurRadius: 6, offset: const Offset(0, 2)),
                ],
              ),
              child: Icon(item.icon, color: iconColor, size: 22),
            ),
            const SizedBox(height: 10),
            Text(
              item.title,
              textAlign: TextAlign.center,
              style: const TextStyle(fontWeight: FontWeight.w500, fontSize: 12, color: _green),
            ),
          ],
        ),
      ),
    ),
  );
}

class _FeatureItem {
  final IconData icon;
  final String title;
  final String subtitle; // dipertahankan di class agar tidak breaking, meski tidak lagi ditampilkan
  final VoidCallback onTap;
  final bool isSecondary;
  _FeatureItem(this.icon, this.title, this.subtitle, this.onTap, {this.isSecondary = false});
}
```

**Catatan:** field `subtitle` dipertahankan di class `_FeatureItem`
(supaya tidak breaking di tempat lain yang mungkin memakainya),
tapi **tidak lagi ditampilkan** di card — sesuai gaya grid minimalis
role lain (label saja, tanpa deskripsi).

---

## 5. Yang TIDAK Boleh Diubah

- Header gradient (`_buildHeader`) — tetap seperti sekarang
- Stat Grid & Detail Grid (`_buildStatGrid`, `_buildDetailGrid`) —
  tetap seperti sekarang, style badge tetap dipakai
- `AdminService.getDashboard()` dan logic loading/error
- Routing: `master-data`, `absensi`, `nilai`, `rapor`, `pengaturan`
  key tetap sama, hanya tampilan card yang berubah

---

## 6. Ringkasan Perubahan File

| File | Perubahan |
|---|---|
| `lib/features/admin/dashboard/dashboard_page.dart` | `_buildFeatureGrid()`: 3 kolom, aspect 1:1. `_buildFeatureCard()`: ikon bulat duotone, hapus subtitle. `_FeatureItem`: tambah `isSecondary`, Pengaturan = `true` |

---

## 7. Definition of Done

- [ ] `flutter analyze` tanpa error
- [ ] `flutter build web` sukses
- [ ] Menu Fitur Admin: grid 3 kolom, ikon bulat duotone, 4 menu
      hijau + Pengaturan kuning
- [ ] Header & Stat Grid Admin tidak berubah dari sebelumnya
- [ ] Semua 5 role (Admin, WK, Guru Mapel, Kamad, Guru BK) kini
      punya gaya Feature Grid yang konsisten (grid 3 kolom, ikon
      bulat duotone, tanpa deskripsi)
