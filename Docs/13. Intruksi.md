Redesign my existing application.

The current application uses a Navigation Drawer with a vertical menu list.

Replace the old navigation with a modern responsive Dashboard.

Keep all existing features.

Only redesign the user experience.

The Dashboard should become the main page.

Display every feature as Grid Cards.

Maintain the same business logic.

Only improve UI/UX and navigation.

Use Material Design 3.