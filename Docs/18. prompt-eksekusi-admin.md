# Prompt Eksekusi — Dashboard Admin

```
TASK: Ubah layout "Menu Fitur" di dashboard Admin dari list-row 
menjadi grid ikon bulat duotone, agar konsisten secara visual 
dengan dashboard role lain (WK, Guru Mapel/Wali Kelas, Kepala 
Sekolah, Guru BK) yang sudah lebih dulu diubah.

KONTEKS PENTING: Dashboard Admin TIDAK memakai shared widget 
DashboardTemplate seperti role lain — ini implementasi custom 
sendiri di dashboard_page.dart miliknya. JANGAN pindahkan Admin 
untuk memakai DashboardTemplate (di luar scope, berisiko besar) — 
cukup restyle widget lokal yang sudah ada.

FILE: lib/features/admin/dashboard/dashboard_page.dart

BAGIAN YANG DIUBAH: HANYA method `_buildFeatureGrid()`, 
`_buildFeatureCard()`, dan class `_FeatureItem` di file ini.

JANGAN UBAH: `_buildHeader()` (gradient hijau), `_buildStatGrid()`, 
`_buildDetailGrid()`, `_ModernStatCard`, `_ModernDetailCard`, 
`_StatItem`, `_DetailInfo` — semua ini tetap seperti sekarang.

PEMBAGIAN WARNA (5 menu, Pengaturan jadi pengecualian kuning 
karena sifatnya konfigurasi sistem, beda dari 4 menu monitoring 
operasional lain):
- Master Data → hijau
- Absensi → hijau
- Nilai → hijau
- Rapor → hijau
- Pengaturan → KUNING

GANTI class _FeatureItem dari:
class _FeatureItem {
  final IconData icon;
  final String title;
  final String subtitle;
  final VoidCallback onTap;
  _FeatureItem(this.icon, this.title, this.subtitle, this.onTap);
}

Menjadi (tambah isSecondary, subtitle dipertahankan di class tapi 
tidak lagi ditampilkan di card):
class _FeatureItem {
  final IconData icon;
  final String title;
  final String subtitle;
  final VoidCallback onTap;
  final bool isSecondary;
  _FeatureItem(this.icon, this.title, this.subtitle, this.onTap, {this.isSecondary = false});
}

GANTI list features di _buildFeatureGrid() dari:
final features = [
  _FeatureItem(Icons.storage_outlined, 'Master Data', 'Kelola data master', () => widget.onFeatureTap?.call('master-data')),
  _FeatureItem(Icons.calendar_today_outlined, 'Absensi', 'Monitoring absensi', () => widget.onFeatureTap?.call('absensi')),
  _FeatureItem(Icons.grading_outlined, 'Nilai', 'Monitoring nilai', () => widget.onFeatureTap?.call('nilai')),
  _FeatureItem(Icons.description_outlined, 'Rapor', 'Monitoring rapor', () => widget.onFeatureTap?.call('rapor')),
  _FeatureItem(Icons.settings_outlined, 'Pengaturan', 'Pengaturan sistem', () => widget.onFeatureTap?.call('pengaturan')),
];

Menjadi (tambah isSecondary: true HANYA di Pengaturan):
final features = [
  _FeatureItem(Icons.storage_outlined, 'Master Data', 'Kelola data master', () => widget.onFeatureTap?.call('master-data')),
  _FeatureItem(Icons.calendar_today_outlined, 'Absensi', 'Monitoring absensi', () => widget.onFeatureTap?.call('absensi')),
  _FeatureItem(Icons.grading_outlined, 'Nilai', 'Monitoring nilai', () => widget.onFeatureTap?.call('nilai')),
  _FeatureItem(Icons.description_outlined, 'Rapor', 'Monitoring rapor', () => widget.onFeatureTap?.call('rapor')),
  _FeatureItem(Icons.settings_outlined, 'Pengaturan', 'Pengaturan sistem', () => widget.onFeatureTap?.call('pengaturan'), isSecondary: true),
];

GANTI grid delegate di _buildFeatureGrid() — dari responsif 
(1/2/3 kolom tergantung lebar) menjadi 3 kolom tetap, aspect 
ratio persegi:
GridView.builder(
  gridDelegate: const SliverGridDelegateWithFixedCrossAxisCount(
    crossAxisCount: 3,
    crossAxisSpacing: 12,
    mainAxisSpacing: 12,
    childAspectRatio: 1.0,
  ),
  shrinkWrap: true,
  physics: const NeverScrollableScrollPhysics(),
  itemCount: features.length,
  itemBuilder: (_, i) => _buildFeatureCard(features[i]),
)

GANTI _buildFeatureCard() dari layout Row (ikon kotak kiri + 
judul/subtitle + chevron kanan) menjadi Column tengah (ikon bulat 
duotone di atas, label di bawah, tanpa subtitle, tanpa chevron):

Widget _buildFeatureCard(_FeatureItem item) {
  final gradient = item.isSecondary
    ? [const Color(0xFFFFF8E1), const Color(0xFFFFE082)]
    : [const Color(0xFFE8F5E9), const Color(0xFFA5D6A7)];
  final iconColor = item.isSecondary ? const Color(0xFFE65100) : const Color(0xFF1B5E20);
  final shadowColor = item.isSecondary ? _yellow : _green;

  return Material(
    color: _white,
    borderRadius: BorderRadius.circular(16),
    child: InkWell(
      borderRadius: BorderRadius.circular(16),
      onTap: item.onTap,
      child: Container(
        decoration: BoxDecoration(
          borderRadius: BorderRadius.circular(16),
          border: Border.all(color: _green.withOpacity(0.1)),
        ),
        padding: const EdgeInsets.all(12),
        child: Column(
          mainAxisAlignment: MainAxisAlignment.center,
          children: [
            Container(
              width: 48, height: 48,
              decoration: BoxDecoration(
                shape: BoxShape.circle,
                gradient: LinearGradient(
                  begin: Alignment.topLeft,
                  end: Alignment.bottomRight,
                  colors: gradient,
                ),
                boxShadow: [
                  BoxShadow(color: shadowColor.withOpacity(0.12), blurRadius: 6, offset: const Offset(0, 2)),
                ],
              ),
              child: Icon(item.icon, color: iconColor, size: 22),
            ),
            const SizedBox(height: 10),
            Text(
              item.title,
              textAlign: TextAlign.center,
              style: const TextStyle(fontWeight: FontWeight.w500, fontSize: 12, color: _green),
            ),
          ],
        ),
      ),
    ),
  );
}

ATURAN KERJA:
1. Scope HANYA dashboard_page.dart milik Admin, bagian Menu Fitur
2. JANGAN ubah header, stat grid, detail grid
3. JANGAN ubah AdminService atau logic loading/error
4. JANGAN ubah key routing (master-data, absensi, nilai, rapor, 
   pengaturan) — hanya tampilan card
5. Tunjukkan diff sebelum eksekusi
6. flutter analyze + flutter build web harus sukses
7. Test tampilan mobile & desktop, pastikan 3 kolom tetap rapi

OUTPUT: diff file, konfirmasi build sukses tanpa error.
```
